package MPS.sandbox.Model;

/*Generated by MPS */

import java.util.List;
import java.util.Scanner;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.ArrayList;

public class Project {
  private String name;
  private String version;
  private List<Build> builds;
  private List<Task> tasks;

  public Scanner scanner = new Scanner(System.in);

  public Project(String name, String version) {
    this.name = name;
    this.version = version;
    this.builds = ListSequence.fromListAndArray(new ArrayList<Build>(), new Build("Build1", "model.group", "1"));
    this.tasks = ListSequence.fromListAndArray(new ArrayList<Task>(), new Task("Task1", "desc", "model.group", "false"));
  }

  public void exec() {
    System.out.println("Project" + "Model_Proj" + ":");
    System.out.println("\t version:" + "1");

    Integer option = -1;
    System.out.println("\nList of Builds");

    while (option != 0) {
      int index = 2;
      for (Build b : builds) {
        System.out.format("%d - %s", index, b.getName());
        index++;
      }
      System.out.println("0 - Exit");
      System.out.print("\n Choose an option: ");
      option = scanner.nextInt();

      if (option != 0) {
        System.out.println("Invalid Option!");
      }
    }
  }

}
